<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="moondraft.Pages.SettingsPage"
             Visual="Material"
             xmlns:sv="clr-namespace:AiForms.Renderers;assembly=SettingsView"
             BackgroundColor="{StaticResource BackgroundColor}">
    <ScrollView>
        <StackLayout Spacing="0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Label x:Name="darkThemeSwitchLabel" Grid.Row="1" Grid.Column="1" HorizontalOptions="End" Text="Enable Dark Theme" TextColor="{StaticResource TextColor}"></Label>
                <Switch x:Name="darkThemeSwitch" Grid.Row="1" Grid.Column="2" IsToggled="{Binding EnabledDarkTheme}" />
            </Grid>
        </StackLayout>
    </ScrollView>
</ContentPage>
